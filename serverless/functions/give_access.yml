handler: handlers.access_handler.handle
events:
  - http:
      path: access/{dataset_id}
      method: put
      parameters:
        paths:
          dataset_id: true
      authorizer: ${file(serverless/kc-authorizer.yaml)}
