handler: handlers.permission_handler.update_permission
events:
  - http:
      path: permissions/{dataset_id}
      method: put
      request:
        parameters:
          paths:
            dataset_id: true
      authorizer: ${file(serverless/kc-authorizer.yaml)}
