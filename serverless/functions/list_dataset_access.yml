handler: handlers.access_handler.list_access
events:
  - http:
      path: access
      method: get
      request:
        parameters:
          querystrings:
            dataset_id: false
            team_id: true
            scope: false
      authorizer: ${file(serverless/kc-authorizer.yaml)}
