handler: event_stream.create_event_stream.handle
environment:
  STAGE: ${self:provider.stage}
events:
- http:
    path: event-stream/{dataset_id}/{version}/
    method: post
    cors: true
    request:
      parameters:
        paths:
          dataset_id: true
          version: true
